<div class="bliptv">
	<div  class="video">
		If you don't see the video, watch it <a href="<?= $this->item->getLink(); ?>">here</a>.	
		<?= $this->item->getEmbed(); ?>
	</div>
	<?php if ( strlen( trim( $this->item->getDescription() ) ) > 0 ) : ?>
		<div class="note">
			<?= $this->item->getDescription(); ?>
		</div>
	<?php endif ?>
</div>